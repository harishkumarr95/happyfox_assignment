#rules file name (give the full path if the file is not in the same directory)
rules_filename = 'rules.json'

#database credentials
database_host = 'localhost'
database_user = 'root'
database_password = 'root'
